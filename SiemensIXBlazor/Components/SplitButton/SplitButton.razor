@* -----------------------------------------------------------------------
// SPDX-FileCopyrightText: 2024 Siemens AG
//
// SPDX-License-Identifier: MIT
//
// This source code is licensed under the MIT license found in the
// LICENSE file in the root directory of this source tree.
// -----------------------------------------------------------------------
*@

@using Microsoft.JSInterop;
@using SiemensIXBlazor.Enums.Button;
@using SiemensIXBlazor.Helpers;
@inject IJSRuntime JSRuntime
@namespace SiemensIXBlazor.Components
@inherits IXBaseComponent

<ix-split-button @attributes="UserAttributes"
				 id="@Id"
				 label="@Label"
				 aria-label-button="@AriaLabelButton"
				 aria-label-split-icon-button="@AriaLabelSplitIconButton"
				 split-icon="@SplitIcon"
				 disabled="@(Disabled ? "true" : null)"
				 disable-button="@(DisableButton ? "true" : null)"
				 disable-dropdown-button="@(DisableDropdownButton ? "true" : null)"
				 enable-top-layer="@(EnableTopLayer ? "true" : null)"
				 icon="@Icon"
				 style="@Style"
				 class="@Class"
				 close-behavior="@(EnumParser<CloseBehavior>.EnumToString(CloseBehavior))"
				 variant="@(EnumParser<ButtonVariant>.EnumToString(Variant, replaceUnderscores: true))">
	@ChildContent
</ix-split-button>
