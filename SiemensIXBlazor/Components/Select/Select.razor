@* -----------------------------------------------------------------------
// SPDX-FileCopyrightText: 2024 Siemens AG
//
// SPDX-License-Identifier: MIT
//
// This source code is licensed under the MIT license found in the
// LICENSE file in the root directory of this source tree.
// -----------------------------------------------------------------------
*@
@using Microsoft.JSInterop;
@using SiemensIXBlazor.Enums.Select;
@using SiemensIXBlazor.Helpers;
@inject IJSRuntime JSRuntime
@namespace SiemensIXBlazor.Components
@inherits IXBaseComponent
<ix-select @attributes="UserAttributes"
           allow-clear="@(AllowClear ? "true" : null)"
           aria-label-clear-icon-button="@AriaLabelClearIconButton"
           disabled="@(Disabled ? "true" : null)"
           editable="@(Editable ? "true" : null)"
           enable-top-layer="@(EnableTopLayer ? "true" : null)"
           value="@(Value is string stringValue ? stringValue : null)"
           aria-label-add-item="@AriaLabelAddItem"
           i18n-placeholder="@I18nPlaceholder"
           i18n-placeholder-editable="@I18nPlaceholderEditable"
           i18n-select-list-header="@I18nSelectListHeader"
           i18n-no-matches="@I18nNoMatches"
           i18n-more-items="@I18nMoreItems"
           i18n-all-selected="@I18nAllSelected"
           i18n-remove-selected-item="@I18nRemoveSelectedItem"
           mode="@(EnumParser<SelectMode>.EnumToString(Mode))"
           id="@Id"
           readonly="@(Readonly ? "true" : null)"
           style="@Style"
           class="@Class"
           hide-list-header="@(HideListHeader ? "true" : null)"
           dropdown-max-width="@DropdownMaxWidth"
           dropdown-width="@DropdownWidth"
           helper-text="@HelperText"
           info-text="@InfoText"
           invalid-text="@InvalidText"
           valid-text="@ValidText"
           warning-text="@WarningText"
           collapse-multiple-selection="@(CollapseMultipleSelection ? "true" : null)"
           label="@Label"
           name="@Name"
           required="@(Required ? "true" : null)"
           show-text-as-tooltip="@(ShowTextAsTooltip ? "true" : null)">
    @ChildContent
</ix-select>
